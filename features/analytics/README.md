# `features/analytics`

**Single responsibility:** the analytics surface — warehouse operational metrics (Company-facing) and unified cross-marketplace analytics (Client-facing), plus CSV/PDF exports and scheduled email reports.

**Lands in:** Phase 5.

Reads from the operational entities; never duplicates state.
